<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Latest Trending News</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-yellow-100 min-h-screen p-8">
  <div class="max-w-4xl mx-auto">
    <h1 class="text-3xl font-bold text-blue-950 mb-8">Latest Trending News</h1>
    <ul class="space-y-6">
      <% articles.forEach(article => { %>
        <li class="p-6 bg-yellow-200 rounded-lg shadow-md hover:bg-yellow-300 transition">
          <h3 class="text-2xl font-semibold text-blue-950">
            <a href="<%= article.url %>" class="hover:underline"><%= article.title %></a>
          </h3>
          <p class="text-blue-950 mt-4"><%= article.description %></p>
        </li>
      <% }) %>
    </ul>
  </div>
</body>
</html>
